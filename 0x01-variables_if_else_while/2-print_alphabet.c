#include <stdio.h>
#include <stdlb.h>
#include <time.h>
/**
 * main - Betty Style
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
		putchar(low);
		printf("\n");
	return (0);
}
